{
    "scripts": {
        "frontend:install": "cd reactnd-contacts-complete && pwd && npm install",
        "backend:install": "cd reactnd-contacts-server && pwd && npm install",
        "frontend:build": "cd reactnd-contacts-complete && pwd && npm run build",
        "backend:build": "cd reactnd-contacts-server && pwd && npm run build"
    }
}